```
git -v
git --version
git config --global user.name
git config --global user.email
git config --list
cd 
mkdir
git init
git add .
git commit -m
git push
git diff
git status
git log
git log --oneline
git checkout id
clear
ls
ls -
git reset --soft HEAD^


```

```
git reset --soft HEAD^
```